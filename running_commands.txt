# Archived shutdown notes

This project's public Cloud Run/Phoenix deployment was shut down after the
hackathon. Do not redeploy without intentionally restoring credentials and
setting:

```bash
ALLOW_ARCHIVED_DEPLOY=true
DISABLE_LLM_CALLS=false
```

Local no-LLM defaults:

```bash
DISABLE_LLM_CALLS=true
PUBLIC_DEMO_MODE=true
AGENT_MODE=local
```

Historical GCP project boundary:

```text
self-healing-agent  project_id=gen-lang-client-0880901302  project_number=65778886250
old demo backend    project_id=my-pulseroom-project        project_number=274002881656
```
